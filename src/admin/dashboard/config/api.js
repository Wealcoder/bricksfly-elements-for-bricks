// Set an endpoint here once its REST API exists; leave it null to keep
// using the static fallback data from src/admin/dashboard/config/data/*.
export const API_ENDPOINTS = {
  tutorials: null,
  documentation: null,
  whatsNew: null,
  blogs: null,
};
